import cv2
import numpy as np

изображение = cv2.imread("image.png", cv2.IMREAD_GRAYSCALE) 

_, изображение_порог = cv2.threshold(изображение, 150, 255, cv2.THRESH_BINARY)

высота, ширина = изображение.shape

центр_x, центр_y = ширина // 2, высота // 2
квадрат_начало_x, квадрат_начало_y = центр_x - 75, центр_y - 75
квадрат_конец_x, квадрат_конец_y = центр_x + 75, центр_y + 75

roi = изображение_порог[квадрат_начало_y:квадрат_конец_y, квадрат_начало_x:квадрат_конец_x]

if np.any(roi == 255):  
    print("🛰️ Метка обнаружена в центральной области. Инвертируем изображение...")
    изображение_порог = cv2.flip(изображение_порог, -1)  

cv2.rectangle(изображение_порог, (квадрат_начало_x, квадрат_начало_y), 
              (квадрат_конец_x, квадрат_конец_y), (200, 200, 200), 2)

cv2.imshow("Обработанное изображение", изображение_порог)
cv2.waitKey(0)
cv2.destroyAllWindows()

